CREATE TABLE hired_employees (
    id INTEGER,
    name STRING,
    hire_datetime STRING,
    department_id INTEGER,
    job_id INTEGER
);

CREATE TABLE departments (
    id INTEGER,
    department STRING
);

CREATE TABLE jobs (
    id INTEGER,
    job STRING
);